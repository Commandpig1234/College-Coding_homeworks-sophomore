/*递归 ：函数内直接/间接调用自身
确定问题、递归分解、确定终止条件



*/

#include <bits/stdc++.h>
/*long long fib_result[100]; 记忆化   
long long fib(int n){
	if (n == 0 || n == 1)
		return 1;
	if (fib_result[n] > 0)
		return fib_result[n];
	fib_result[n] = fib(n - 1) + fib(n - 2);
	return fib_result[n]; 
		
}

int main(){
	int n=0;
	scanf("%d",&n);
	printf("%lld",fib(n));	
	
	return 0;
}
*/

//Bunny Breeding


int main(){
	
	
	return 0;
}
